import { IndexViewService } from './index-view.service';

describe('IndexViewService', () => {

  let service: IndexViewService;

  it('should instantiate.', () => {
    service = new IndexViewService();
  });

});
